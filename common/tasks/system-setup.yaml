- name: copying service file
  ansible.builtin.template:
    src: "{{ componet }}.service.j2"
    dest: /etc/systemd/system/{{ componet }}.service
  notify: daemon reload

- name: strat and enable service
  ansible.builtin.service:
    name: "{{ componet }}"
    enabled: yes