- name: copying service file
  ansible.builtin.template:
    src: "{{ component }}.service.j2"
    dest: /etc/systemd/system/{{ component }}.service
  notify: demon reload

- name: strat and enable service
  ansible.builtin.service:
    name: "{{ component  }}"
    state: restarted
    enabled: yes
